#include <stdio.h>

/**
 * main -  prints the alphabet in lowercase
 * using putchar function
 * Return: 0
 */
int main(void)
{
	int = 97;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	putchar('\n');

	return (0);
}
